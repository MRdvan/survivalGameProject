
    public class chase /*: State*/
{
    //    public State doState(PlayerController controller)
    //    {
    //        controller.playerMovement.chasing();
    //        controller.playerMovement.moving();
    //        controller.playerMovement.running();

    //        if (controller.playerMovement.target != null && controller.playerCollisions.isReached == false)
    //        {
    //            return controller.chaseState;
    //        }
    //        else if(controller.playerMovement.target != null && controller.playerCollisions.isReached)
    //        {
    //            return controller.attackState;
    //        }
    //        else if (controller.playerMovement.target == null && controller.playerMovement.isMoving())
    //        {
    //            return controller.moveState;
    //        }
    //        else
    //        {
    //            return controller.idleState;
    //        }
    //}

}

